{
    "context": [
      "content/website-copy.md"
    ],
    "commands": [
      {
        "name": "Generate Website Pages from Markdown",
        "trigger": "onCommand",
        "run": "npx cursor generate-pages content/website-copy.md --framework nextjs --output app/(marketing)"
      },
      {
        "name": "Generate Components from Markdown Sections",
        "trigger": "onCommand",
        "run": "npx cursor split-md content/website-copy.md --output components/marketing --naming 'Section_${title}'"
      },
      {
        "name": "Build Page Routes",
        "trigger": "onCommand",
        "run": "echo 'Creating page routes...' && mkdir -p app/(marketing) && echo 'export default function Page() { return <Home /> }' > app/(marketing)/page.tsx"
      }
    ],
    "notes": [
      "Each ## heading in website-copy.md becomes a component.",
      "Cursor automatically converts Markdown to MDX-compatible sections.",
      "Use 'Generate Website Pages from Markdown' to scaffold Next.js routes.",
      "Use 'Generate Components from Markdown Sections' to build reusable components.",
      "After generation, run 'vercel --prod' to redeploy your updated site."
    ]
  }